## 布尔值就是完整的答案

当**用户的回答本身就是全部重点**时，布尔响应就是正确的模型。删除确认、「放弃
未保存的修改？」、「你是否同意？」——调用方不需要拿回任何数据，只需要一个是或否。

如果对话框里有表单字段，就直接返回表单数据（参见[输入提示框](/zh-cn/examples/prompt-input)
或[保存表单](/zh-cn/examples/save-form)），而不是一个布尔值再配一条旁路信息。
