---
"@retronew/call-vue": minor
---

Add `@retronew/call-vue`, a Vue 3 port of react-call's `createCallable` API — call and await Vue components (confirmations, dialogs, toasts) like async functions, via `call`/`upsert`/`end`/`update`.
